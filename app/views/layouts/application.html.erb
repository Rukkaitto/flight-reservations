<!DOCTYPE html>
<html>
  <head>
    <title>FlightReservations</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav>
      <span><%= link_to "Flights", flights_path %></span>
      <% if user_signed_in? %>
        <span><%= link_to "My reservations", reservations_path %></span>
        <span><%= link_to "Log out", destroy_user_session_path, method: :delete %></span>
      <% else %>
        <span><%= link_to "Log in", new_user_session_path %></span>
        <span><%= link_to "Sign up", new_user_registration_path %></span>
      <% end %>
    </nav>
    <div class="content">
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <%= yield %>
    </div>
  </body>
</html>
